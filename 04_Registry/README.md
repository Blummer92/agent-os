# Registry

Registry files define inheritance, module versions, aliases, ownership, responsibilities, and reusable capability records.

`reusable-capabilities.yml` is the canonical reusable capability registry governed by `01_Shared_Standards/global-engineering/reusable-capability-registry-standard.md`.
