# Regression contract — #2472

Pre-PR validation command identities and reason codes must contain canonical nonblank text. Whitespace-only and leading/trailing-whitespace identities must fail closed rather than entering exact validation plans.
