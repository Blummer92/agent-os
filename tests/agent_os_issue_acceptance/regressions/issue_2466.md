# Regression contract — #2466

Post-mutation lifecycle readback must remain bound to the exact operation/projection identity admitted before mutation. A different projection for the same PR and issue must fail closed rather than proving convergence.
