# Regression contract — #2463

A finite batch candidate whose PR is already merged at reacquisition must still enter linked-issue post-merge reconciliation when a canonical linked implementation issue exists. Closed-unmerged and unlinked already-terminal PRs must preserve existing fail-closed/idempotent behavior.
