# Regression contract — #2468

The guarded Git-object publication model must represent ordinary non-executable (`100644`) and executable (`100755`) blob modes while rejecting unsupported modes. This is the canonical mode-preserving seam required by #2454.
