# Regression contract — #2470

Guarded Git compare snapshots must reject internally contradictory status/counter/change evidence. In particular, an `identical` comparison must contain zero ahead/behind/commit/change/addition/deletion evidence.
