# Changelog — 0.1.1-draft
- Added live execution-surface capability preflight to ChatGPT Orchestrator for #1039: reuses #918 executor-route semantics and current environment/surface capability evidence, treats missing local tooling such as `gh` as capability-mismatch evidence rather than repository-issue failure, and preserves #986 internal continuation while authorization, source of truth, ownership, and bounded scope remain applicable. No new router, runner, scheduler, workflow, credential path, production capability, external-write capability, or authority was added.
- Updated Fast/Safe Lane validation policy for #1018: focused local validation is the default developer loop, one clean exact-head GitHub aggregate may satisfy the required full-suite validation, and Ready-for-Review still requires all required exact-head checks. Safe Implementation Lane delegates the detailed validation contract to canonical `01_Shared_Standards/global-engineering/testing-and-release.md`.
- Added bounded Routine Notion Write Authorization in `00_Governance/write-authorization-policy.md` (#933): standing authorization covers only specified non-governed teacher-planning, lesson-candidate, working-knowledge, handoff/comment, non-authoritative attachment, and already-approved linked-view navigation writes; sensitive student/private data remains per-action approval-gated; canonical Agent OS ownership/alias/overlay/standard checks, destination/field/source-of-truth/audit-safety verification, and existing stricter overlays/standards remain fail-closed. No governed fields, schemas, permissions, destructive actions, bulk/scheduled/inference-driven writes, or production authorization are added.
- Bumped the Agent OS Execution Service module contract from `0.4.0` to `0.5.0` for AOS-CHATGPT2 (#918): added pure deterministic executor routing with exactly four routes, the finite twelve-value routing-only `ExecutorCapability` vocabulary, immutable `ExecutorRouteDecision` and shared `ExecutorHandoff`, bounded opaque upstream identities, canonical serialization and content-addressed IDs, governed-runner preference, explicit external-fallback gating, fail-closed human-decision overrides, and fixed no-side-effect/non-authorizing behavior. Retired the proposed aggregate capability-evidence model and separate runner/fallback handoff models. Added focused tests; no runner, checkpoint persistence, workflow, credential, protected setting, production, external write, merge, auto-merge, or issue closure occurred.
- Updated Safe Implementation Lane to 0.2.0 for #986: registered-owner transitions are internal routing while current authorization, source of truth, and bounded scope remain applicable; ChatGPT Orchestrator preserves GitHub Service Agent write ownership, QA validation ownership, fail-closed excluded-surface stops, and non-authorizing continuation language while returning one consolidated routine result. Added Orchestrator fixtures and executable policy regression tests. No workflow, protected-setting, credential, production, Scheduler, Notion, Drive, classroom-artifact, or other external-system changes.
- Added Issue Quality Taxonomy 0.1.0 (#883): shared issue-family rules plus pure offline completeness, reference, blocker/unblock, and acceptance-quality checks that reuse canonical `CheckResult` / `Status`; findings are report-only and create no lifecycle or write authority.
- Added backward-compatible `curriculum-visual-asset-compatibility-v2` and `curriculum-visual-asset-candidates-v2` contracts for #871, preserving v1 behavior and adding governed cohesion metadata required by #851. The pure local contracts perform no external access or visual generation and grant no production or classroom-use authority.
- Packaged the canonical Workflow Scheduler for governed root imports (AOS-AUTO1C-P1, #912): added `08_Tooling/workflow-scheduler/pyproject.toml` (standard setuptools `src`-layout metadata, `name = "workflow-scheduler"`, version `0.9.1` matching the existing registry entry, no console entry point, no dependency duplication of the package's own `requirements.txt`) and added `-e ./08_Tooling/workflow-scheduler` to `requirements-dev.txt`, so a single `python -m pip install -r requirements-dev.txt` -- the same command every existing root, CI, and Codespaces install path already runs -- makes `workflow_scheduler` a standard editable install with no `sys.path` mutation, `PYTHONPATH` hack, or private filesystem loader. No CI workflow file changed: `agent-os-validation.yml` already runs `pip install -r requirements-dev.txt` first, so it now installs the package automatically. Added the focused `tests/test_workflow_scheduler_packaging.py` proving root import of `workflow_scheduler`, `DraftTaskProposal`, and `build_draft_task_proposals`; resolution to the single canonical `08_Tooling/workflow-scheduler/src/workflow_scheduler` tree with no duplicate installed path; no subprocess, network, or filesystem-write side effect at import time; and no circular import between `scripts/**` and `workflow_scheduler` (verified by AST-scanning every `scripts/**/*.py` file for an actual import statement, not a `__module__` string match). No public import path, package namespace, Workflow Scheduler runtime behavior, WSC3 proposal semantics, or Scheduler concurrency changed; Issue #752 was not implemented. Full aggregate suite: 3626 tests pass; root `tests/agent_os_candidate_packet` remains 174 passed; `08_Tooling/workflow-scheduler/tests` remains 1234 passed; `validate-repo-structure.sh` reports 9 passed, 0 failed.
- Added the pure `ExecutableLaneSelection` 1.0 selector (`agent-os-executable-lane-selection`, AOS-QUEUE2, #864, approved in #855): `scripts/agent_os_candidate_packet/executable_lane_selection.py` classifies every supplied issue -- given its canonical `IssueOperationalState` (#862) and `AgentOperatingModeDecision` (#863) -- into exactly one of `ready-for-implementation`, `ready-for-review`, `waiting-for-authorization`, `waiting-for-dependency`, `merged-needs-closure`, `needs-reconciliation`, `planning-only`, `terminal`, `invalid`, then selects up to `requested_lane_count` (1-3) productive lanes. Precedence: explicitly requested executable issues in caller-supplied order, then other independently executable issues, then permitted `ready-for-review` issues, then reconciliation work, then planning-only; waiting and terminal issues stay visible but never consume a lane. Ties break on explicit request position, then ascending dependency depth, then ascending issue number -- no timestamp, popularity, model score, or live GitHub state is consulted. Lane replacement only substitutes for an explicitly requested issue that lands in a blocked queue, only when both `substitution_allowed=True` and the candidate's own `substitutable=True`; the substitute is the next eligible independent (`ready-for-implementation`) issue, recorded as a `ReplacementRecord` with a finite reason code, and an exact-required (`substitutable=False`) blocked issue is never silently replaced. Multiple active primary claims classify `needs-reconciliation`, are recorded as a `DuplicateClaimFinding`, and are always excluded from lane selection regardless of lane count or substitution policy; exactly one active primary claim is excluded from new implementation and reaches `ready-for-review` only when the supplied mode evidence permits review. The immutable result carries schema name/version, campaign ID, requested lane count, substitution policy, sorted supplied issue numbers, sorted-unique source `state_id`/`decision_id` identities (never the nested upstream records), every issue's `QueueClassification`, `selected_lanes`, `deferred_candidates`, `replacement_records`, per-issue `RankEvidence` (rank, tier, explicit-request position, dependency depth), `duplicate_claim_findings`, finite `reason_codes`, and fixed `execution_authorized=False` / `side_effects_performed=False`, with deterministic canonical JSON, a domain-separated SHA-256 `selection_id`, strict schema/version/unknown-field rejection, and a byte-stable round trip. Performs no GitHub, network, filesystem, subprocess, Scheduler, provider, credential, production, or external-system call, and no issue/label/branch/commit/PR/comment/merge/closure/repository mutation; queue placement never widens, infers, or collapses an authority field the supplied inputs already carry. Exported from `scripts/agent_os_candidate_packet/__init__.py` alongside the package's existing stage exports. 46 focused tests cover every queue outcome, all three lane counts, both replacement boundaries, duplicate-claim exclusion, explicit-order/dependency-depth/issue-number tie-breaking, stale/conflicting/partial/malformed/unknown-field/unsupported-version/tampered-identity evidence, canonical serialization, and the no-authority/no-side-effect guarantees; full focused and aggregate suites pass.
- Bumped Global Engineering from 0.2.0 to 0.3.0 for #894: added an inherited mobile terminal and Cloud Shell operator-experience rule requiring a brief explanation before commands, one fastest-safe copy-and-paste command when practical, fail-closed chaining, common-state detection, narrow-screen readability, and explicit safeguards against copying prompts, terminal output, incomplete heredocs, or Python source directly into Bash. Updated the common overlay, module version map, and focused inheritance tests. No workflow, protected-setting, credential, production, billing, or external-system change.
- Implemented the `agent-os-codespaces-v1` persistent GitHub Codespaces environment profile (#891, approved in #857): `.devcontainer/devcontainer.json` (personal-account, 2-core Linux, Python 3.11 base image plus the `github-cli` devcontainer feature, `local-only` default network mode) and `.devcontainer/post-create.sh` (deterministic, fail-closed bootstrap). Added `scripts/agent-os-environment-health.py`, focused tests, and `docs/AGENT_OS_CODESPACES_RUNBOOK.md`. No workflow, Scheduler, credential, production, protected-setting, or external-system change. The #972 extension bumps the Agent OS Codespaces Profile from `0.1.0` to `0.2.0` for #972 and adds bounded execution-surface identity, canonical UTC observation time, a surface/freshness-bound content-addressed evidence ID, explicit process-execution evidence, finite tool availability states, and finite GitHub-auth states while preserving legacy booleans and all authority-false semantics; no connector/browser/Drive/Notion/provider probing, credential mutation, Scheduler change, workflow/protected-setting change, production action, external write, merge, auto-merge, or issue closure occurred.
- Added the AOS-RESUME1A execution-checkpoint implementation for #895, based on the design approved in #858: immutable checkpoint records, content-addressed append-only storage with fail-closed quarantine, binding invalidation, mutation-intent identities, and deterministic non-authorizing resume planning. See `scripts/agent_os_execution_checkpoint/README.md`.
- Added the pure `AgentOperatingModeDecision` 1.0 evaluator (`agent-os-operating-mode-decision`, #863, approved in #859): `scripts/agent_os_issue_acceptance/operating_mode.py` intersects a requested mode's capability ceiling (`planning`, `build`, `draft-pr`, `review`, `release`) with the canonical `IssueOperationalState` from `issue_operational_state.py` (#862) and caller-supplied `EnvironmentCapabilityEvidence` (local-execution and push capability), consuming the state's six existing authority projections directly rather than re-deriving or duplicating a second authorization framework. Any non-`ready` operational-state outcome hard-caps progress at `planning` and reports `needs-decision` (needs-decision states), `invalid` (invalid states), or `blocked` (every other degraded state) rather than a successful `planned` outcome, whichever mode was requested; otherwise the evaluator walks `implementation -> draft-pr -> review -> merged -> closed` one gate at a time (implementation authorization + verified local execution, then verified push, then Ready-for-Review authorization, then merge authorization, then closure authorization), stopping at the first unmet gate and reporting it through bounded `mode.*`/`state.*`/`authorization.*`/`environment.*`/`lifecycle.*`/`compatibility.*` reason and blocker codes. An omitted or unrecognized requested mode -- including compatibility phrases such as `complete`, `finish`, and `do everything` -- always defaults to `planning` and never implies `release`. The immutable result preserves requested/effective mode, outcome, maximum-permitted and highest-completed lifecycle stage, next permitted action (anchored on the highest completed stage and capped by the maximum permitted stage, so it is never also reported as prohibited), prohibited actions, required human authorizations and environment capabilities, the source `IssueOperationalState` ID, supplied authorization/environment evidence IDs, all six authority projections unchanged, and `side_effects_performed=false`, with deterministic canonical JSON, sorted set-like fields, domain-separated SHA-256 semantic identity, strict schema/version/unknown-field rejection, and a byte-stable round trip. Performs no GitHub, network, filesystem, subprocess, environment, credential, or Scheduler I/O and creates no authority; a requested mode is a capability ceiling only and never broadens an authority field the supplied `IssueOperationalState` already carries. Not exported from the package `__init__.py` facade, consistent with this package's existing direct-module-import convention for new interfaces. `tests/agent_os_issue_acceptance/test_architecture_boundaries.py` classifies `operating_mode.py` in its own exact `mode` domain, placed downstream of `approval` (whose canonical projection contracts the module consumes) and upstream of `reporting`, which `mode` must not import and which no upstream domain may import from; authority preservation is verified for all six projections (implementation, ready-for-review, execution, merge, closure, external-write) against each source projection's `effective()` value, for both not-authorized and stale-binding evidence. The full focused and aggregate suites pass.
- Bumped `workflow-scheduler` from `0.9.0` to `0.9.1`: connected individual approval stop-condition helpers to Phase 2A approval engine. Helpers now accept optional `source_of_truth_db` parameter and clear blocks only when an explicit `ApprovalDecision.APPROVED` record exists, matching `check_all_stop_conditions` semantics. Extended `check_production_mode` to cover both `production_ready` flag and `TaskMode.PRODUCTION`. Refactored approval lookup into shared helpers (`_requires_approval`, `_lookup_approval`, `_is_approved`) to avoid duplication. Fail-closed posture preserved: absence of approval record is never approval; legacy databases treated as "no approval on record"; all pre-existing callers retain exact blocking behavior. Added eight test cases covering approved/pending/rejected decisions, Production-mode gap, legacy-database tolerance, and ungoverned paths. Updated `docs/API_REFERENCE.md`. Full suite: 1195 tests pass, `validate-repo-structure.sh` reports 7 passed, 0 failed.
- Added LP Pacing Handoff Contract 0.1.0, including the provider-neutral packet, bounded route references, independent authority dimensions, diagnosis mapping, and illustrative pacing cases (#648).
- Added LP Reason Code Catalog 0.1.0, including finite LP semantic reason families, explicit diagnosis identities, and producer/consumer ownership rules (#711).
- Added LP Notion Working Layer 0.1.0, including the proposed-not-authorized Change Request, explicit per-property state contracts, six separate diagnosis properties, bounded pilot records, and shadow-mode-only safeguards (#652).
- Updated `04_Registry/module-version-map.md` to register the LP Pacing Handoff Contract, LP Reason Code Catalog, and LP Notion Working Layer at version 0.1.0.
- Updated Notion Standards to version 0.3.0 for the LP Notion Working Layer standard and governed projection requirements.
- Updated Instructional Design Standards to version 0.7.0 for the LP Pacing Handoff and LP Reason Code standards.
- Added the pure `IssueOperationalState` 1.0 projection with separate authority dimensions, deterministic content identity, strict round-trip validation, finite fail-closed reasons, and adapters for existing approval, merge-authorization, and lifecycle-admission evidence (`#862`, approved in `#853`).
- Reverted the premature merge of #826 (EIA2 pure-local OCR extraction adapter) in #827, backing out merge commit `4c7326b` via `git revert -m 1`. #826 merged ahead of the authorization gates recorded in #713 and #716, and ahead of its own stated handoff condition; see those issues for the governing authorization state and `00_Governance/write-authorization-policy.md` for the policy. Removes `src/instructional_evidence_intake/ocr_adapter.py`, `src/instructional_evidence_intake/models.py`, and `tests/test_instructional_evidence_ocr_adapter.py`, and restores `src/instructional_evidence_intake/__init__.py` to its pre-#826 export surface (`alignment` + `interpreter` only). The reverted work is preserved unchanged at commit `b5c3744` on branch `claude/issues-to-prs-8tztva` for re-landing once those gates clear. Authorization-sequence correction only: no judgment about the reverted code's quality, and no dependency, label, or issue-status change accompanies it.
- Added the Teacher Decision Studio (#823, #824): `01_Shared_Standards/instructional-design/teacher-decision-studio-standard.md` defines the table-first rubric/assessment consultation protocol (locked interaction model, format catalog, explanation/support scale, disagreement behavior, non-goals) and resolves the #823 ownership question -- `Assessment Agent / Student Evidence Coach` is a Notion-side governance ownership label in `00_Governance/agent-os-governance-v1-baseline.md` with no GitHub overlay, so rubric/assessment consultation routes to the already-registered Teacher Modeling Coach (explanation-risk analysis) and Instructional Materials Coach (final rendering) instead of inventing a new agent. `teacher-decision-studio-previews-standard.md` adds the #824 per-option in-chat worksheet preview and labeled PDF preview requirements, source-worksheet retrieval rules, and destination/storage boundaries (no GitHub storage, no auto-overwrite, no implied production authorization). Wired into `chatgpt-orchestrator.md` (0.1.3, routing), `teacher-modeling-coach.md` (0.4.1, explanation analysis), and `instructional-materials-coach.md` (0.4.8, preview/PDF ownership) as inherited standards, with new test scenarios in the matching `07_Agent_Tests/*.tests.md` files. Documentation only; no agent invented, no production-gate or write-authorization change.
- Added the Artifact-First Response Standard (#821): `01_Shared_Standards/instructional-design/artifact-first-response-standard.md` requires classroom-material responses to lead with the requested artifact, preview, or content specification before backend routing and governance reporting, while preserving existing gate, ownership, source-of-truth, and stop-condition behavior; a blocked-production response may still show a safe, clearly labeled preview. Wired into `chatgpt-orchestrator.md` (0.1.3), `agent-orchestrator.md` (0.1.1), and `instructional-materials-coach.md` (0.4.8) as an inherited standard, with new test scenarios in the matching test files. Documentation only.
- Added Rubric Language Requirements to the Student Language Standard (#822, `01_Shared_Standards/instructional-design/student-language-standard.md` 0.2.0 -> 0.3.0): first-person student rubric wording instead of teacher-observer phrasing, separated teacher scoring guidance, complete-rubric and weighting visibility instead of showing only a rotating/changed row, and next-step guidance on beginning-level scores. Added the required "Rubric completeness & weighting" ship gate to `material-quality-rubric.md` (0.2.0 -> 0.3.0) and a matching focused QA test to `07_Agent_Tests/qa-test-agent.tests.md`; `qa-test-agent.md` (0.1.2) now inherits both standards. Also corrected a pre-existing version-drift: `module-version-map.md`'s Student Language Standard row (`0.1.0`) had fallen out of sync with the file's own `0.2.0`; both now read `0.3.0`. Documentation only.
- Split `04_Registry/module-version-map.md`'s longer per-module descriptions (Workflow Scheduler, Agent Memory & Context Budget Manager, IA4D-to-Scheduler Handoff Contract, Agent OS Execution Service) into a new `04_Registry/module-version-map-details.md` to keep the index under the 100-line limit while adding the new Artifact-First Response Standard and Teacher Decision Studio Standard rows. No version or content changed, only relocated.
- Picked up the deferred non-blocking CBP3 adapter code-review cleanup (#819, from PR #818's CodeRabbit review, none merge-blocking): `scripts/agent_os_cloud_build_provider/__init__.py` now exports `MAX_RECONCILIATION_MATCHES_INSPECTED` and `MAX_DIAGNOSTIC_LENGTH`. `adapter.py` switches the runtime `isinstance()` check from `typing.Mapping` to `collections.abc.Mapping` (Ruff UP035), freezes `CloudBuildSubmissionRequest.provider_metadata` into a `MappingProxyType` in `__post_init__` as defense-in-depth, documents that `unavailable`/`internal-error` submission outcomes require the injected client to have already proven no build was created (else it must report `ambiguous`), and documents that the adapter never sleeps between polls so the injected client owns poll pacing. `README.md`'s adapter-behavior paragraph is now a bullet list (at-most-once submission/identity transport, bounded polling, the `denied` shape and diagnostic/`merge_authorized` guarantees, reconciliation matching). In `tests/agent_os_cloud_build_provider/`: extracted the duplicated `sys.path` bootstrap (also present in `test_core.py`) into a new package-scoped `conftest.py`, matching the existing `tests/agent_os_issue_acceptance/conftest.py` convention; `_configuration_variant()` now asserts its recomputed fingerprint is non-empty instead of relying on `""` differing from any real value; `FakeCloudBuildClient.observe()` now raises `AssertionError` on an unconfigured fixture (`observe_results=[]`) instead of silently producing an `IndexError` the adapter's own exception handling would turn into a legitimate-looking `UNKNOWN` result; `test_invalid_or_drifted_invocation_submits_zero_times` now asserts `last_diagnostic` per parametrized branch to prove which guard fired; added direct coverage for the two constructor `TypeError` guards (non-protocol-conforming client, non-exact `CloudBuildProviderConfiguration`); and the two hardcoded `512` diagnostic-length assertions now import and assert against `MAX_DIAGNOSTIC_LENGTH`. The `ast`-based import-allowlist test now allows `collections` and `types`. All 90 package tests and the full non-preexisting-broken suite (1785 tests) pass; `validate-repo-structure.sh` passes. No behavior change to submission, observation, reconciliation outcomes, or public result shapes.
- Bumped `agent-os-execution-service` from `0.3.0` to `0.4.0` and added the PILOT-VALIDATION additive pre-PR validation-planning contract (#723) that lets validation-only candidate #726 carry one immutable issue- and invocation-bound validation subject, validation plan, and command plan without fabricating a pull request. `scripts/agent_os_remote_validation/models.py` adds the frozen, slotted, keyword-only `PrePrValidationSubject` (`agent-os-pre-pr-validation-subject` `1.0`) binding repository `Blummer92/agent-os`, issue `726`, invocation ID, base branch `main`, base SHA, non-protected branch, expected source SHA, tested SHA, sorted-unique non-overlapping `allowed_files` and `forbidden_paths`, ordered-unique `required_command_identities`, approval ID and positive revision, projection ID, SHA-256 implementation-contract fingerprint, and `execution_mode=validation-only`; plus `PrePrValidationPlan` (`agent-os-pre-pr-validation-plan` `1.0`) carrying the subject, selector version, ordered commands, command-set digest, reason codes, and the frozen 30-second per-command and 300-second total validation ceilings with `remote_build_required`, `execution_authorized`, `merge_authorized`, and `side_effects_performed` fixed `False`. Construction fails closed on malformed or missing identifiers, non-lowercase or short SHAs, protected or base-equal branches, tested/source SHA drift, unsorted or duplicate canonical tuples, overlapping scope, non-`726` issue numbers, wrong execution mode, and timeouts above the ceilings; a missing pull request is never represented as `0`, a dummy PR, a copied identity, or an unbound mutable branch. `selector.py` adds `select_pre_pr_validation_plan(...)`, `serialize_pre_pr_validation_subject(...)`, `pre_pr_validation_subject_id(...)`, `serialize_pre_pr_validation_plan(...)`, and `pre_pr_validation_plan_id(...)` (`sha256("agent-os-pre-pr-validation-plan:v1\0" + canonical-json)`, prefix `pre-pr-validation-plan:`), all pure-local and raising bounded errors instead of degrading to manual-review; and `_focused_matches(...)` now gives an exact-path rule precedence over a prefix rule for the same path, which is required so the new exact rule does not collide with the pre-existing `08_Tooling/workflow-scheduler/` prefix rule. Precedence intentionally masks prefix-owner conflicts for any path that has an exact owner, because such a conflict cannot change that path's outcome; a path with no exact owner still fails closed to `rule.ambiguous`, two conflicting exact owners still fail closed, and selection stays independent of both rule order and changed-file order. `validation_profiles.yml` adds the one frozen focused rule `workflow-scheduler-concrete-runtime-adapters` for `08_Tooling/workflow-scheduler/tests/test_concrete_runtime_adapters.py`, and `command_planning.py` allowlists that exact command to the frozen argv `("python", "-m", "pytest", "08_Tooling/workflow-scheduler/tests/test_concrete_runtime_adapters.py")` and adds one explicit exact-type pre-PR branch that rejects drift in repository, `issue:<issue_number>` identity, base branch, base SHA, branch, expected source SHA, allowed and forbidden scope, request fingerprint, ordered command identities, unregistered commands, duplicates, and timeout ceilings. Positive-PR command-plan behavior is unchanged and characterized by new golden tests: for equivalent command-plan inputs the `ValidationCommandPlan` canonical payload and `command-plan:` identity are byte-for-byte and identity-for-identity what they were at `df2d7d61` across the static, focused, aggregate, and multi-command union profiles, and `COMMAND_REGISTRY_VERSION` deliberately stays `1.0` because allowlisting a command is additive. Selection behavior is unchanged except for exactly two outcomes caused by the new focused rule, both fail-safe supersets that skip no validation: (1) a changed-file set consisting only of `08_Tooling/workflow-scheduler/tests/test_concrete_runtime_adapters.py` now selects the narrow focused rule instead of the whole `python -m pytest 08_Tooling/workflow-scheduler/tests` suite; and (2) a changed-file set combining that exact path with prefix-covered Workflow Scheduler sibling paths now reports `profile.focused-union` instead of `profile.focused-package` and unions both commands, so the union can contain a narrow command already subsumed by the broader package command and the pilot file is validated twice. Both cases change the validation-plan profile, command set, command-set digest, and `validation-plan:` identity for those changed-file sets, and command subsumption is deliberately not suppressed in this change. Every other shipped `SelectionInput`/`ValidationPlan` outcome, canonical payload, and `validation-plan:` identity is byte-for-byte and identity-for-identity what it was at `df2d7d61`, and the pre-PR selector rejects the mixed set outright because the matched command set no longer equals the subject's bound command identities. `pyproject.toml` and `EXECUTION_SERVICE_VERSION` move to `0.4.0`; request, result, fingerprint, command-plan, and command-registry schema versions are unchanged; Workflow Scheduler remains `0.9.0` with no Workflow Scheduler file changed. Planning only: candidate #726 was not executed, Scheduler concurrency remains `0`, and no execution or merge authorization, workflow, remote build, provider, credential, network, package installation, retry, persistence, publication, issue mutation, or other external-write behavior was added.
- Bumped `agent-os-execution-service` from `0.2.0` to `0.3.0` and added the WSC6B4 governed validation-execution composition boundary (#697): `08_Tooling/agent-os-execution-service/src/agent_os_execution_service/execution_composition.py` adds `EXECUTION_COMPOSITION_SCHEMA_VERSION` (`1.0`), `ExecutionAuthorizationEvidence`, `ExecutionCompositionStatus`, `ExecutionCompositionResult`, and `compose_and_run_validation(...)`. The function consumes a caller-supplied exact `ExecutionServiceRequest`, exact `ValidationCommandPlan`, explicit `ExecutionAuthorizationEvidence`, caller-supplied evaluated time, canonical `SingleIssuePilotInput`, and canonical `ConcreteRuntimeConfiguration`; revalidates request/plan/authorization/configuration identity (fingerprint, repository, expected SHA, plan and validation-plan IDs, argv-to-`FrozenTestCommand` matching, and `ConcreteRuntimeConfiguration.verify(...)`); and, only once every identity matches, delegates exactly once to the existing `run_concrete_runtime_entrypoint_with_validation_evidence(...)` Workflow Scheduler entrypoint. It returns/retains the exact canonical `FrozenTestValidationResult` from `ConcreteRuntimeExecutionOutcome.validation_result` without rerunning validation, implements no duplicated per-command evidence model, no command loop, no early-stop controller, and no second process runner, executor, worktree manager, lease, scheduler, retry, or command registry. `merge_authorized` is always false; no generic pass boolean is exposed; focused success remains `focused-pass-aggregate-pending` with `aggregate_pending=true`, and only canonical aggregate success may report `aggregate-pass`. A pass status additionally requires the canonical runtime status to be exactly `completed`: a passing `FrozenTestValidationResult` is produced before teardown, so a `quarantined`, `failed`, `timed-out`, `blocked`, `stale`, `needs-decision`, or unknown runtime status — including a run whose workspace cleanup, metadata cleanup, or lease release failed after validation passed — is reported as bounded `infrastructure-failure`/`manual-review` evidence with the canonical quarantine, cleanup, and release reason codes preserved, never as focused or aggregate success. Identity mismatches, an unsupported profile, or an unexpected return shape fail closed to bounded `manual-review`/`infrastructure-failure` evidence before any process runs; ordinary `Exception` subclasses raised by the delegated entrypoint are converted the same way, while `KeyboardInterrupt`, `SystemExit`, and `GeneratorExit` propagate uncaught. `08_Tooling/agent-os-execution-service/src/agent_os_execution_service/__init__.py` exposes the new names lazily via module `__getattr__` so importing the package never requires the Workflow Scheduler package to be on `sys.path` unless a caller actually uses the composition surface; existing request/inspection/command-planning imports and tests are unchanged. `08_Tooling/agent-os-execution-service/tests/test_execution_composition.py` adds focused unit coverage plus a bounded integration path built on the real current-main `ConcreteRuntimeConfiguration`, validation-only mode, and `ScenarioGitRunner` test fixture, autospecced signature-drift checks for `validate_execution_service_request`, `validation_command_plan_id`, `ConcreteRuntimeConfiguration.verify`, and `run_concrete_runtime_entrypoint_with_validation_evidence`, and an architecture-boundary test proving no direct `subprocess`/`os.system`/`os.popen`/`asyncio.create_subprocess_*` call and no competing lease/retry/queue/scheduler/parser/runner/validator/command-registry definition exists in the new module. `pyproject.toml` and `EXECUTION_SERVICE_VERSION` both move to `0.3.0`; request, result, fingerprint, command-plan, and command-registry schema versions are unchanged; Workflow Scheduler remains `0.9.0`. No stable Workflow Scheduler file changed. No AI invocation, retry, GitHub/workflow/provider mutation, credential, network, package installation, persistence, concurrency, merge, or other external-write behavior was added.
- Bumped `agent-os-execution-service` from `0.1.0` to `0.2.0` (WSC6B4A, #719): updated `pyproject.toml`, `EXECUTION_SERVICE_VERSION`, `04_Registry/module-version-map.md`, and the README to truthfully describe a phased execution-service package. The README now states that a later, separately authorized composition layer (#697) may consume an exact `ExecutionServiceRequest`, an exact `ValidationCommandPlan`, caller-supplied execution authorization, caller-supplied Workflow Scheduler runtime configuration, the canonical validation-only runtime, and the exact retained `FrozenTestValidationResult`; that any later validation execution must use only exact fixed argv from `ValidationCommandPlan`, remain local and bounded, run each command at most once, introduce no retry, publish nothing, and reuse the existing Workflow Scheduler lease, worktree, validation, containment, cleanup, release, quarantine, and evidence-return lifecycle; and that validation-only mode supplies no executor, supplies no executor argv, constructs no `PosixProcessExecutor`, and creates no no-op or synthetic executor. Execution authorization, command execution, validation success, review status, Ready-for-Review, and merge authorization remain separate states. This change is package-contract and documentation only: no `compose_and_run_validation(...)` or other executable composition function was added, and no runtime, retry, AI invocation, GitHub/workflow/provider mutation, concurrency, credential, network, or other external-write behavior was introduced. Workflow Scheduler remains `0.9.0`, unchanged.
- Added the WSC5B6 additive canonical validation-only runtime mode (#707): `SingleIssuePilotInput.execution_mode` and `ConcreteRuntimeConfiguration.execution_mode` select `standard` (unchanged) or `validation-only` on the one existing `_drive_lifecycle(...)` sequence. Validation-only reuses the same lease, worktree creation/inspection, cancellation checkpoints, frozen-test validator, changed-path containment, cleanup, release, quarantine, and #698 evidence-return contracts, and branches only around executor dispatch and the post-executor path check. It dispatches no executor (`executor_dispatch_attempts = 0`, `executor_called = false`, `executor_started = false`), never claims termination confirmation, constructs no `PosixProcessExecutor`, adds no no-op executor, and runs each bound validation command at most once while returning the exact retained `FrozenTestValidationResult`. The mode is bound into the canonical pilot result identity, runtime observation, and configuration fingerprint; unsupported, drifted, or mismatched modes and misplaced executor authority fail closed before any adapter runs. Standard-mode signatures, lifecycle behavior, and completion invariants are unchanged. No second lifecycle, runner, validator, scheduler, queue, retry, persistence path, workflow, provider call, network behavior, credential, AI invocation, concurrency, or arbitrary shell execution was added.
- Added the WSC5B5 bounded concrete-runtime evidence return contract (#698): `ConcreteRuntimeExecutionOutcome` and `run_concrete_runtime_entrypoint_with_validation_evidence(...)` return the canonical `SingleIssueRuntimeOutcome` together with the exact immutable `FrozenTestValidationAdapter.last_result`, or explicit `None` when validation was not reached. The existing runtime-only entrypoint remains backward compatible and delegates through the same single construction and invocation path. No validation rerun, retry, scheduler, workflow, provider, credential, persistence, publication, AI invocation, concurrency, or external write was added.
- Added closed-issue authority and prior-scope readiness safeguards (#544, under parent risk owner #543). Extended `01_Shared_Standards/github/issue-lifecycle-standard.md` with a Closed-Issue Authority section (the open body is authoritative while active and synchronized immediately before closure, then immutable historical evidence; the closed state, closure reason, latest dated final-disposition comment, and canonical successor/duplicate pointer control routing; historical `status:ready`/`status:blocked`/`status:needs-decision` labels cannot reactivate a closed issue; `completed`, `completed/no-change`, `not planned`, `duplicate`, and `superseded` are distinct terminal dispositions; new related work opens a new issue and reopening requires explicit authorization plus the same original objective) and a Refactor And Consolidation Value section (a shared abstraction normally needs at least three behaviorally identical callers, net nonblank lines are supporting evidence only after helper/import/test/docs overhead, unrelated helpers cannot be combined to cross a threshold, no universal 40-line threshold exists, and no-change is a valid successful disposition). Added a required `prior-scope-review` and an optional `refactor-evidence` textarea to `.github/ISSUE_TEMPLATE/agent-os-task.yml`, matching compatibility sections to `03_Templates/issues/agent-os-build-issue.md`, and the intake and migration semantics to `01_Shared_Standards/github/issue-acceptance-automation.md`. `evaluate_issue_readiness()` now runs one deterministic, report-only `prior scope review` check that passes only on visible content under the canonical `Prior scope, duplicate, and supersession review` heading for every tier and otherwise fails closed to `needs-decision` (never `blocked`) — covering `_No response_`, blank, quoted, fenced, HTML-commented, unrelated, and absent evidence — mirroring the documentation-impact legacy migration and never parsing live links. Recorded #543 as the single persistent canonical risk owner for issue-lifecycle authority and retired-scope revival in `04_Registry/risk-owner-map.md`; #544 is not added as an owner and is expected to close after implementation. Existing documentation-impact readiness behavior is unchanged, refactor evidence remains conditionally required by policy rather than globally mandatory in readiness, and no workflow, label-map, protected-branch, repository-setting, RC6, capability-registry, agent-overlay, Scheduler, Cloud Build, or external-system change was made.
- Added the informational reusable-capability evidence adapter for issue readiness (RC5B / #470) under the binding #248 contract (composing #471 provenance, #472 report compatibility, #475 constructor compatibility, and corrected #476 import isolation). A new `scripts/agent_os_issue_acceptance/reuse_readiness.py` adapter attaches caller-supplied RC3 `DiscoveryResult` evidence and corrected-RC4 `ValidationReport` evidence to an already-computed `ReadinessResult` as a strictly informational layer: it adds only `AcceptanceReport.informational_checks` (new immutable `tuple[CheckResult, ...]` field, `repr=False`, appended after `remaining_risks`) and never changes `ReadinessOutcome`, `overall_status`, ordinary checks, blockers, ordinary manual-review items, or `exit_code_for()`. `render_report()` renders a separate `Reusable-capability evidence (informational):` section only when populated, preserving byte-for-byte legacy output when empty. Provenance is compared using caller-supplied `RegistryProvenance` values only (strict, version-aware equality; the adapter never reads the registry or recomputes canonicalization); missing, mismatched, or unsupported provenance, failing/contradicted validation, and conflicting or malformed evidence all suppress positive reuse guidance while leaving base readiness unchanged. The adapter is the sole cross-package boundary (direct-submodule imports only, no invocation of `RegistryReader`/discovery/validation/serialization orchestration) and is not exported from `__init__.py`; `readiness.py` is unchanged and base readiness imports/runs without the reusable-capability package installed. Deterministic and mutation-free; matching provenance proves only same-snapshot identity, not correctness, compatibility, ownership, approval, readiness, or authorization. No `readiness.py`, `__init__.py`, registry, label-projection, issue-form, workflow, Cloud Build, Scheduler, package-metadata, or external change.
- Added the corrected RC4 report-only reusable-capability registry validator (#494) under the binding #254 contract: new `validation.py` (orchestration, structural/lifecycle/successor checks, provenance acquisition/comparison, ownership/exemption) and `inspection.py` (bounded, offline, `ast`-only path/interface/consumer/test evidence — no runtime imports, no whole-repository scanning), immutable `EvidenceConfidence`/`ValidationSeverity`/`ValidationEvidence`/`ValidationFinding`/`ValidationReport` models in `models.py` with a single `from_findings` construction path (`report_version` 1.0, aggregation `fail>manual-review>warn>pass`, no pass findings), a canonical validation serializer in `serialization.py`, and a separate JSON-only `agent-os-capability-validate` command (`validation_cli.py`; exit `0`/`1`/`2`/`3`). Every finding carries independent confidence and severity; the report binds the same-snapshot `RegistryProvenance` reused from #482 (never recomputed) and fails closed on unsupported/mismatched supplied provenance. Package version `0.1.0`→`0.2.0`; `report_version` `1.0` and `registry_version` `0.1.0` remain distinct. Report-only and mutation-free: no registry content/schema, discovery, provenance, `reader.py`, `__init__.py` facade, readiness, Scheduler, workflow, Cloud Build, or external change.
- Implemented the canonical reusable-capability registry provenance path (#482) defined by the #471 contract: a new `provenance.py` canonicalizes validated parsed records (not raw YAML bytes) into a deterministic, offline SHA-256 digest (`algorithm registry-canonical-records`, version `1`) over compact canonical JSON (`ensure_ascii=False`, `sort_keys=True`, `(",", ":")` separators, UTF-8), with records sorted by `capability_id`, governed set-like lists sorted by exact Unicode code point (duplicates fail closed, never silently deduplicated), exact case/Unicode/path spelling preserved, and `registry_version` bound in. Added an immutable, version-aware `RegistryProvenance` model (strict digest shape, fail-closed `require_supported`), attached optional equal provenance to every `DiscoveryResult` from one snapshot via `discover_capabilities(..., attach_provenance=True)` without changing ranking, confidence, warnings, or ordering, and extended the canonical serializer to emit the provenance envelope only when populated (legacy output preserved byte-for-byte when absent). Malformed YAML, unsupported versions, unknown fields, duplicate capability IDs, duplicate set-like values, unknown top-level keys, and duplicate YAML mapping keys all produce no provenance. Matching provenance proves only same-snapshot identity — not correctness, freshness, compatibility, approval, readiness, or execution authorization. No `reader.py`, registry content, schema version, CLI, package-facade, workflow, readiness, Scheduler, or external change was made.
- Added a bounded PyYAML compatibility matrix for the reusable-capability registry duplicate-key loader (#491): a focused GitHub Actions workflow runs the merged #489 reader contract at the minimum declared PyYAML (`6.0`), the ordinary-CI-resolved version, and the newest compatible `<7` release, recording each exact resolved version and failing closed (never green) if a lane resolves out of range or collects no tests. Added a focused `test_pyyaml_compatibility.py` asserting the loader API surface and duplicate-key/alias/merge boundary, and restored the essential contract-bearing comments (null-valued top-level key rejection, composed/decomposed Unicode distinctness, the 14-field #481 coverage inventory, the inventory guard, and the minimal-record fixture rationale) in `test_reader.py`. All three lanes passed (`6.0` and `6.0.3` observed locally); the declared `PyYAML>=6.0,<7` range was retained unchanged. Test, workflow, and documentation changes only — no `reader.py`, registry content, schema, provenance, dependency-range, readiness, or external change.
- Hardened the reusable-capability `RegistryReader` to fail closed on duplicate set-like registry values (#481): every governed collection field now rejects exact duplicates after the existing whitespace trim, identifying the capability and field, while preserving exact case, Unicode code points, and path spellings as distinct values. No silent deduplication, normalization, registry mutation, readiness change, or external I/O was added.
- Hardened the reusable-capability `RegistryReader` to fail closed on unknown top-level registry keys (#486): only `registry_version` and `capabilities` are accepted at the document top level, and any other key is rejected with an identifying error, mirroring the existing record-level unknown-field rejection. This prevents a mistyped `capabilities` key from silently loading zero records and keeps every accepted registry change representable in provenance. No normalization, silent ignoring, registry mutation, readiness change, or external I/O was added.
- Added the IDB2D immutable portable approved-execution projection (#407), reverifying WSC3 proposal identity, approval record and applicability, IssuePlan current state, and GEX repository evidence before producing deterministic timestamp-independent semantic identity and canonical JSON. The projection preserves source-head and tested SHAs, evidence type, cohorts, contract fingerprints, allowed files, forbidden paths, and required tests while remaining derived, read-only, non-authoritative, and non-execution-authorizing. No retrieval, persistence, Scheduler runtime, task, queue, lease, workspace, worker, dispatch, concurrency, production write, credential access, or external I/O was added.
- Migrated issue-acceptance policy, readiness, IA5 documentation readiness, and legacy preflight onto the canonical all-candidates-first IssuePlan scanner (#358). `parse_issue_metadata()` remains a parser-free compatibility projection; malformed, duplicate, conflicting, partial, inaccessible, unsupported, stale, unknown-field, version, and identity findings fail closed with bounded scanner evidence. No approval, Scheduler runtime, workflow mutation, repository setting, production write, or external I/O was added.
- Added the three-level issue lifecycle standard (`01_Shared_Standards/github/issue-lifecycle-standard.md`): roadmap issue, implementation issue, and pull request contracts; child-issue creation test; issue-body maintenance policy; closure/supersession policy; and minimal label policy with documented legacy aliases. Added the canonical risk-owner map (`04_Registry/risk-owner-map.md`), a pull-request template matching the existing acceptance-report headings, and structural contract tests (`tests/test_issue_lifecycle_contract.py`). Documentation, template, registry, and test changes only; no readiness, approval, authorization, exact-SHA, fail-closed, or automation behavior was changed.
- Added immutable, content-bound IssuePlanCore approval records and deterministic applicability evaluation (#398), including verified WSC3 proposal bindings, exact IssuePlanCore/GEX evidence identities, append-only decision revisions, explicit expiry-at-boundary behavior, bounded invalidation reasons, and false authorization/side-effect invariants. No Scheduler approval storage, task, queue, lease, workspace, adapter, dispatch, persistence, live lookup, clock read, production write, or external I/O was added.
- Added WSC3 immutable, stateless draft-proposal ingestion (#362), consuming public WSC1, IssuePlanCore, and GEX evidence; preserving exact upstream outcomes and reason codes; keeping source-head and synthetic-tested SHAs distinct; binding proposals to repository, digest, evidence, contract, and node identities; and fixing authorization and side-effect fields to false. No task, approval, queue, lease, workspace, worker, adapter execution, dispatch, persistence, system-clock read, credential access, or external I/O was added.
- Added the GEX1B immutable, pure-local capability and repository-state evidence surface (#361), including canonical schema serialization, bounded fail-closed reason codes, exact SHA/evidence-type binding, worktree-state classification, secret-like value suppression, and offline regression coverage. No live probe, authentication, environment mutation, approval, Scheduler behavior, or external I/O was added.
- Added the IDB2A immutable, pure-local IssuePlanCore current-state evidence surface (#360), including complete scanner candidate-set identity, deterministic fingerprints, explicit absent/null/omitted/unavailable field states, bounded current/stale/blocked comparisons, approved reason codes, and fail-closed regression coverage. No approval, execution authorization, Scheduler behavior, external I/O, or mutation was added.
- Implemented the WSC1 pure local IA5A handoff serializer and validator (#331), including byte-exact canonical JSON, graph/planning-result/handoff SHA-256 digests, supported-version and invariant checks, bounded fail-closed evidence, immutable public models, and offline regression coverage. No Workflow Scheduler runtime, execution authorization, concurrency, or external I/O was added.
- Amended ADR 0002 to 0.2.0 (IA5A follow-up, #325): fixed a byte-exact canonical serialization rule (removed the conflicting sort_keys/dataclass-order statement), enumerated the complete `graph_digest` field payload, stated classification precedence directly instead of referencing the private `_ORDER` constant, added the `handoff_digest` envelope field and its digest rule, defined `planning_result_version` ownership/location/bump policy, and reconciled `module-version-map.md` wording with the contract's already-registered, not-yet-implemented status. Planning and documentation only.
- Added ADR 0002 (IA5A, #325): the versioned, immutable, non-executable `SchedulerPlanningHandoff` envelope, classification boundary, serialization/digest rules, and freshness/revalidation/approval-binding contract between IA4D supplied-graph planning and Workflow Scheduler. Planning and documentation only; no serializer, validator, adapter, task creation, approval storage, or dispatch implemented.
- Recorded the documentation-impact issue-intake contract merged by #305, including the canonical form fields, documentation values, bounded path guidance, and report-only downstream readiness handoff.
- Hardened RC3 reusable capability discovery with exact non-keyword lookup semantics, result-specific keyword provenance and confidence, lowercase-kebab capability-ID enforcement, collision-safe indexes, deterministic executable regression coverage, and Cloud Build installation for the package.
- Added the offline reusable capability registry reader and deterministic discovery package under `08_Tooling/reusable-capability-registry/`, with immutable records, detached JSON serialization, CLI/module interfaces, conservative confidence semantics, and fixture-backed tests.
- Seeded five evidence-backed reusable capabilities, added deterministic registry validation, and added exact acceptance-report output regression coverage.
- Added the Global Engineering 0.2.0 reusable capability registry contract, initialized the empty canonical registry, routed ownership and validation responsibilities, and registered both new files in the Documentation Dependency Map.
- Added Dashboard Migration Verification standard and reusable tooling scaffold under `08_Tooling/dashboard-migration-verification/` with sanitized examples, placeholder snapshot generation, dependency graph validation, conservative report output, and tests.
- Added Workspace Automation Builder sync handoff and dry-run receipt schemas, sanitized fixtures, and a local-only fixture validator.
- Strengthened the Issue #98 Documentation Dependency Map into an index plus companion documents (`00_Governance/documentation-dependency-map.md` and `00_Governance/documentation-dependency-map/`), added a non-authoritative machine-readable `metadata.yaml`, connected it to Issue #97, defined measurable documentation-quality metrics and coverage-validation questions, and added a repository-path validation check to `07_Agent_Tests/validate-repo-structure.sh`.
- Added Unit Alignment handoff service and test samples to the Workspace Automation Builder tooling sample fixture.
- Added Workspace Automation Builder Apps Script tooling scaffold, dedicated sync safety bridge tests, and sanitized Unit Alignment handoff fixture.
- Added Workspace Automation Builder as a Google Workspace shared standard and prompt template; wired it into the Google Workspace Automation Engineer overlay without creating a new agent.
- Folded durable Workspace Automation Developer behavior into Workspace Automation Builder route selection, tests, prompt template, and legacy alias registry.
- Added Agent OS Governance v1.0 baseline as the canonical source of truth for dashboard ownership, readiness vocabulary, production routing, production authorization, historical aliases, production safety rules, and governance change policy.
- Updated dashboard governance standards to document the canonical ownership model, official production pipeline, standard dashboard structure, and Governance v1.0 readiness vocabulary.
- Clarified that Instructional Materials Coach owns material quality only, Production Control is the only production authorization owner, and Assessment Agent / Student Evidence Coach are one canonical owner.
- Recorded the Notion Sprint 1 implementation as operational documentation: governance alias notes, Instructional Materials Coach routing links, Production Control standardization, Daily Generation Packet standardization, Teacher Modeling Coach standardization, and Assessment Agent / Student Evidence Coach standardization.
- Added Teacher Modeling Misunderstanding Audit template for formal lesson/model audits grounded in task, supports, gaps, teacher moves, and priority revisions.
- Added Misunderstanding Response Designer prompt template for student confusion, causes, visible evidence, teacher moves, prevention revisions, and checks for understanding.
- Added Model Sequence Builder prompt template for tight teacher modeling sequences aligned to the student task.
- Added Teacher Talk Rehearsal prompt template for replacement language, talk tracks, and rehearsal coaching.
- Migrated durable Teacher Modeling Coach behavior for lesson-modeling coaching, teacher-talk rehearsal, read-only audit support, Notion sync boundaries, source freshness, and memory limits.
- Added Instructional Materials Coach design system for material-type selection, game-format guardrails, source questions, image asset context, and final quality checks.
- Added Instructional Materials Coach material design defaults for classroom-ready layout, density, writing load, scaffolding, accessibility, and revision behavior.
- Added Instructional Materials Coach slide deck defaults for deck sequence, projection readability, visual balance, teacher usability, and slide build priorities.
- Added Instructional Materials Coach design-variant pattern standard for reusable visual systems, labels, icons, layouts, color roles, and component patterns.
- Migrated durable Instructional Materials Coach Custom GPT behavior into Agent OS standards, overlay references, and tests without adding a new agent.
- Added the first three Agent OS pilot workflow runbooks and prompts: lesson planning, classroom artifact generation, and repository change handoff.
- Clarified classroom artifact routing so teacher planning and lesson candidates default to Notion handoffs, student-facing materials default to Drive, and GitHub lesson storage requires explicit approval and a GitHub Change Request.
- Added Markdown line-limit exception policy and updated structural validation to honor documented legacy exceptions for Issue #62 without rewriting long archival, tooling, or Python standards docs.
- Added ChatGPT implementation bridge: root `AGENTS.md`, ChatGPT Orchestrator overlay, GitHub Service Agent overlay, GitHub Change Request template, matching overlay tests, registry ownership/responsibility updates, and manual validation notes for Issue #59.
- Docs accuracy: fixed `08_Tooling/workflow-scheduler/docs/` (USER_GUIDE, ARCHITECTURE, API_REFERENCE) still calling shipped Phase 2A-2E features unimplemented; also fixed a stale diagram and missing max_workers docs. Docs only.
- Repo cleanup (PR 3 of 3): moved four completed Workflow Scheduler planning docs out of the repo root into `06_Archive/workflow-scheduler-planning/` -- `PART_A_FINAL_SUMMARY.md`, `PART_A_TO_PART_B_HANDOFF.md`, `PHASE_0_BASELINE_CORRECTED.md`, `PHASE_B_SCOPE.md`. Files moved, not deleted or rewritten.
- Repo cleanup (PR 2 of 3): documented `08_Tooling/workflow-scheduler/` in `CLAUDE.md`'s `08_Tooling/` Contents list. Short, factual entry only; no scheduler docs rewritten, no scheduler code touched.
- Repo cleanup (PR 1 of 3): deleted six stale one-time GitHub-upload/bootstrap files that referenced ephemeral local staging paths or one-time setup steps from before this repo existed on GitHub. No code or behavior changes.
- Documented a known limitation in `08_Tooling/notion-navigation-client/README.md`: live-sheet structural validation is blocked by a platform-side Claude.ai connector-approval bug; fixture/mock tests remain the only validated test path until resolved externally or run locally with OAuth credentials.
- Added `01_Shared_Standards/notion/notion-navigation-index-standard.md` and `08_Tooling/notion-navigation-client/`, documenting the user-provided Notion navigation-index Google Sheet as a non-authoritative navigation aid and adding a read-only Python client with fixture-backed tests.
- Added an `## Agent Compute Profiles` section to `01_Shared_Standards/instructional-design/production-gates-and-compute.md`, updated three prompt templates, tightened tests, and bumped Instructional Design Standards to 0.5.0.
- Added `00_Governance/agent-os-advisory-mode.md` and a Current Operating Mode section in `CLAUDE.md` so Agent OS is advisory during pilot review for low-risk work while preserving strict approval gates for sensitive writes.
- Added `03_Templates/prompts/daily-agent-shortcuts.md` with low-friction daily lanes for dashboard drafts, QA reviews, Python local fixes, and instructional material drafts without weakening safety gates.
- Gave Instructional Materials Coach a Notion learning loop that writes local lesson-candidate records matching the real Lessons Learned field schema and documented the schema in `01_Shared_Standards/notion/notion-learning-databases.md`.
- Added Instructional Materials Coach as a canonical agent, registered it, added tests, added a prompt-index entry, and added a runnable Python package at `08_Tooling/instructional-materials-coach/` for Slides decks and Docs worksheets from approved templates.
- Fixed `validate-repo-structure.sh` registry and empty-folder checks, added missing module-version records, and fixed a wrong path plus inconsistent shorthand in CLAUDE.md troubleshooting examples.
- Added `07_Agent_Tests/`: compliance test prompts, a shared pass/fail checklist, and `validate-repo-structure.sh` structural regression tests.
- Extracted `02_Agent_Overlays/_common-overlay-rules.md` from duplicated overlay blocks; overlays now reference it instead of repeating shared content.
- Renamed `00_Governance/agent-inheritance-registry.md` to `agent-creation-policy.md` and made `04_Registry/agent-inheritance-registry.md` the sole source for the agent list and inheritance mapping.
- Clarified dashboard sync routing and prevented duplicate standalone Dashboard Sync Agent ownership.
- Retired Apps Script Sync Test Agent as a standalone canonical agent name.
- Preserved Apps Script Sync Test Overlay as specialist sync-validation behavior.
Older release history: see [CHANGELOG-HISTORY.md](CHANGELOG-HISTORY.md).
