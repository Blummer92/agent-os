# Changelog — 0.1.1-draft
- ARCH GitHub client convergence (#2507) — Added one canonical token-backed PyGithub client-construction function to the existing `scripts.agent_os_github_issue_provider.auth` owner and migrated the Git-object CLI, governed PR branch-refresh operator, and execution-service host read transport to consume it. The three callers no longer independently parse `GITHUB_TOKEN`/`GH_TOKEN` and construct `Auth.Token`/`Github` clients; domain-specific Git-object mutation admission, branch-refresh authorization/currentness, and governed-resume issue/authorization semantics remain with their existing owners. Added a narrow single-owner regression test and updated the provider README. No new GitHub transport framework, result hierarchy, HTTP dependency, workflow, credential source, authorization model, merge/closure authority, production effect, or external write is introduced.
- BM1 (#2361) — Added the pure-local finite PR-batch merge-admission planner in `scripts/agent_os_issue_acceptance/pr_batch_merge_plan.py`. The planner preserves owner-specified order after deterministic deduplication, requires exact canonical evidence for every requested PR, distinguishes candidate, already-terminal, item-local-blocked, and shared-blocked dispositions, and structurally requires pre-merge reacquisition for every planned item while hard-coding `merge_authorized=False` and `side_effects_performed=False`. Registered the module in the existing planning architecture domain and added focused regression coverage in `tests/agent_os_issue_acceptance/test_pr_batch_merge_plan.py`. No GitHub retrieval, merge executor, scheduler, queue, checkpoint store, authorization system, workflow/protected-setting change, credential, production/external write, merge, or issue closure is introduced.
- Governed refresh trigger grammar and terminal evidence (#1875) — Completed the bounded workflow-boundary half of #1875 after PR #1888 landed the runner-side receipt preservation. The `refresh_pr` admission guard in `.github/workflows/agent-os-governed-invocation.yml` previously tested `startsWith(github.event.comment.body, '/agent-os refresh-pr ')`, whose trailing space encoded the operand boundary and therefore duplicated — and could diverge from — the canonical anchored grammar `^/agent-os refresh-pr (?P<pr>[1-9][0-9]*)$` in `scripts/agent_os_issue_labels/pr_branch_refresh_actions.py`. The guard is now a documented coarse cost prefilter carrying no operand semantics, and is a proven superset of the canonical grammar, so `pr_branch_refresh_actions.py` is the single owner of operation grammar and every admitted trigger — accepted or rejected — reaches it and yields a precise machine-readable terminal reason instead of persisting silently. The `Publish bounded refresh summary` step additionally surfaces the underlying refresh receipt that PR #1888 began preserving: `Receipt publication HTTP status`, `Underlying refresh status`, `Underlying refresh reasons`, and a `Refreshed head SHA` validated against `^[0-9a-f]{40}$`, rendering only those bounded whitelisted fields so a converged refresh stays attributable when receipt publication fails rather than being hidden behind the outer `needs-decision` status. Token permissions are deliberately unchanged (`contents: write`, `issues: write`, `pull-requests: read`); the observed 403 does not by itself justify widening `pull-requests`, and any permission change remains separately evidence-driven. No second refresh implementation, receipt store, alternate writer, credential path, retry loop, checkout/concurrency change, merge authority, or issue-closure authority was introduced. Regression coverage in `tests/test_agent_os_pr_refresh_actions_workflow.py` pins the admission/canonical-grammar superset relationship and executes the workflow's real summary program against bounded result fixtures.
- Semantic ownership advisory (#1511) — Added `validate_semantic_ownership()` beside the existing `validate()` in `07_Agent_Tests/validate_registry_consistency.py`, closing the blind spot where the Registry Consistency Audit never reads `04_Registry/navigation/**` or any `.yml`/`.yaml` file under `04_Registry/`. Advisory (non-blocking) only: the new focused test always passes, and findings print to stdout without failing `scripts/validate-all.sh`. Scans `04_Registry/navigation/**/*.md` ownership assertions, `04_Registry/lp-reason-code-catalog.yaml`'s `semantic_owners[].role` and family `semantic_owner` fields, and the named owners in `01_Shared_Standards/instructional-design/lp-reason-code-catalog.md`'s `## ownership-single-semantic-owner` section for stale-retired-owner and unknown-canonical-owner findings. Explicitly defers to `reusable_capability_registry`'s own owner validation for `04_Registry/reusable-capabilities.yml` and excludes `06_Archive/**` and the Legacy Agent Alias Registry's own resolution tables. Added the required SO-1 through SO-8 fixture matrix to `tests/test_registry_consistency.py`. No ownership value is reassigned and no `.github/workflows/**` file changed; running the check against current `main` surfaces (without failing) the still-live stale "Integration Manager" assertions in `04_Registry/navigation/README.md`, `04_Registry/navigation/dmsc-apps-script-bundle.md`, `04_Registry/navigation/dmsc/overview.md`, `04_Registry/lp-reason-code-catalog.yaml`, and `01_Shared_Standards/instructional-design/lp-reason-code-catalog.md`.
- BUGSQUASH-2 (#1270) — Made the Agent OS Validation Gate's SHA attribution truthful. The `Publish validation summary` step now reports the exact pull-request head SHA (`github.event.pull_request.head.sha`) and the checked-out merge SHA (`$GITHUB_SHA`) as two separate, distinctly labelled lines, replacing the single ambiguous `Commit:` line that recorded only the ephemeral `refs/pull/N/merge` commit and therefore could not evidence exact-head validation. Reporting only: checkout semantics, `ref:` behavior, permissions, concurrency, and required-check names are unchanged, and which commit gets validated is deliberately left to its own issue. Added `tests/agent_os_issue_acceptance/test_validation_gate_sha_attribution.py` pinning both labelled lines and the unchanged excluded surfaces.
- CKR11 (#1516) — Instantiated the live Lessons Learned activation bridge (`lesson_activation_bridge.py`) that turns bounded live Notion Lessons Learned rows into `LessonRecordEvidence` for the existing CKR6/CKR2 preflight path: known-reference-first then bounded filtered `query_data_source` retrieval (never more than the CKR6 five-candidate budget), deterministic finite-vocabulary row normalization with explicit fail-closed non-ready outcomes for missing/ambiguous Status, Area, Learning Type, or Source Link evidence, and zero Notion reads for `not-needed`. Reuses the existing read-only Notion adapter and the #1520 shared CKR2 candidate-owned provenance invariant with no Lessons-specific duplicate guard. Reconciled the live `Status`/`Lesson ID` schema drift in `01_Shared_Standards/notion/notion-learning-databases.md` and wired the bridge into the ChatGPT Orchestrator's Lessons Learned preflight routing.
- CKR13 (#1520) — Corrected the shared CKR2 sufficiency contract so `sufficient` requires every retained selected `CodingKnowledgeCandidate` to carry its own candidate-owned `canonical_github_refs`; request-owned `canonical_rule_refs` remain task/inspect-first evidence and can no longer mask missing candidate provenance, including in mixed multi-candidate sets. CKR6 and CKR10 inherit the fix without adapter-local duplicate guards, and CKR10 `verification_required` now reflects candidate-backed provenance instead of request-only refs. Added regression coverage across CKR2/CKR6/CKR10 and a `04_Registry/risk-owner-map.md` row naming #1520 the canonical owner of the provenance-ownership risk.
- PPUX-VRL9 / DEVVAL3 (#1495) — Added the Gate C authorized-overlay integrity gate at `08_Tooling/instructional-materials-coach/picture-perfect-coach/src/overlayIntegrity.ts`: only the resolved plan authorizes PPUX-added pixels, a zero-overlay plan authorizes zero overlay pixels, an artifact-only output cannot gain an editor window/panels/toolbars/tutorial UI, plan-derived overlay footprints and masks cannot expand after output, planned target geometry stays tied to admitted target geometry, admitted source-native arrows/badges/chrome remain source evidence, and executor self-report is recorded verbatim but never consulted. #1484 exact-composite planning/execution/validation is consumed unchanged, #1496/#1497/#1501 ownership is not duplicated or widened, and no OCR, computer vision, or model-scored target authority is introduced. Registered the third fixed developer-validation identity `ppux-picture-perfect-ts-vitest` through the existing validation registry, issue-comment ingress grammar, and GCE/IAP transport, binding one repository-owned `node vitest run` argv over six fixed Picture Perfect test paths with no caller-selectable interpreter, package set, package directory, path, filter, glob, reporter, argv, or environment; unknown identities and supplied arguments continue to fail closed. Publishing the DEVVAL3 host runtime remains a separately authorized administrator action that this change does not perform, and all three existing validation identities are behaviorally unchanged. No workflow, IAM/credential, protected-setting, production, Drive, Notion, classroom, merge, or issue-closure surface changed.
- DEVVAL3 — Add fixed Instructional Materials/current-curriculum dev-validation identity (#1454) — Registered the second fixed developer-validation identity `instructional-materials-current-curriculum-suite` through the existing validation registry, issue-comment ingress grammar, and GCE/IAP transport, binding one repository-owned `python -m pytest` argv over fixed Instructional Materials Coach and current-curriculum integration tests. Both existing validation identities remain behaviorally unchanged.
- BUGSQUASH-5 (#1272) — Documentation-only paths now count as covered with zero commands during focused validation selection, preventing a documentation co-change from unnecessarily escalating an otherwise focused plan to aggregate. Existing all-documentation static selection, aggregate-configuration precedence, unmapped-executable fail-safe behavior, and focused command coverage remain unchanged. Added mixed-category, changed-file-order, rule-order, digest, and validation-plan identity regression coverage.
- BUGSQUASH-4 (#1271) — Corrected validation-profile configuration coverage: `requirements-dev.txt` now selects the `aggregate` validation profile; removed the nonexistent `requirements.txt` aggregate-path entry and stale `scripts/run_` aggregate prefix; added configuration-integrity regression coverage requiring every declared `aggregate_paths` entry to exist and confirming the real dependency manifest selects the canonical aggregate plan. No selector algorithm, workflow, provider, credential/IAM, GCE, production, or external-system behavior changed.
- AOS-NCCE4 (#1424) — Added the bounded offline-first Notion Compute Decision writer at `08_Tooling/notion-compute-decision-writer/`. `plan_and_write_compute_decision(...)` consumes an already-serialized #1419 `agent-os-compute-control-projection/1.0` payload (parsed and schema-pinned by `parse_compute_control_projection_evidence`, never imported as a dataclass) and prepares one bounded update plan for the existing Notion `Tasks / Issues` record frozen by the #1420 destination contract: data source `5216eacf-639d-4881-92bc-a634ead56669`, `Compute Decision` (rich text) is the only writable field, and `Source Link` is the identity anchor and is never rewritten. Target resolution uses only an exact `Source Link` match — a title-only match never resolves a row — and zero or more than one match fails closed (`notion-compute-decision-target-missing` / `-target-ambiguous`) without ever creating a new row; the injected `NotionComputeDecisionClient` protocol has no create method at all. A projection whose own #1419 reason codes signal fail-closed currentness, or whose repository/issue/head identity does not match the caller's freshly reacquired `CanonicalIdentity`, is blocked before any target read. Every `ComputeDisposition` value maps deterministically to its exact #1420 presentation string, including `unavailable` → `"Verify Current State"`, itself the fail-closed non-green display value. Already-matching values return `UNCHANGED_SKIP` with no mutation; a transient/rate-limited update outcome is never blindly retried, only reconciled by readback (`UPDATED` if the readback already shows the intended value, otherwise `AMBIGUOUS_WRITE_RESULT`); successful updates require exact page-identity and intended-value readback before being reported verified. `dry_run=True` is the default and makes zero client calls; the package has zero dependencies and mirrors the #959 `visual_asset_notion_writer.NotionClient` read/write shape for this destination only rather than adding a second Notion client or generic sync framework, per the completed #1003 destination-local pre-write reconciliation decision. 32 focused offline tests (`08_Tooling/notion-compute-decision-writer/tests/test_writer.py`) cover exact-identity resolution, title-only-match insufficiency, ambiguous/missing targets, stale/conflicting #1419 evidence, the full disposition-to-presentation mapping, unchanged-skip, unrelated-property preservation (only `Compute Decision` is ever named in an `update_page` call), the field allowlist, ambiguous-write reconciliation without blind retry, readback-mismatch verification failure, determinism, all against an injected in-memory fake client with zero network/Notion/GitHub/Scheduler I/O. No live Notion mutation, schema/view/database change, workflow/Scheduler/Cloud Build change, or new authority was introduced; live mutation remains separately approval-gated under the #1420 Change Request.
